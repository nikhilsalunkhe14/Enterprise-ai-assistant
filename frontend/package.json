{
  "name": "enterprise-ai-assistant",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "axios": "^1.6.0",
    "tailwindcss": "^3.3.0"
  },
  "scripts": {
    "start": "npx http-server . -p 5500 -c-1 --cors -o index.html",
    "build": "echo 'Build not needed for static files'",
    "test": "echo 'Tests not configured'",
    "dev": "npx http-server . -p 5500 -c-1 --cors -o index.html"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
