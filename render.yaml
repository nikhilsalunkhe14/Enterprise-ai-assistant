services:
  - type: web
    name: ai-it-project-assistant
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "cd backend && uvicorn main:app --host 0.0.0.0 --port 7860"
    envVars:
      - key: GROQ_API_KEY
        sync: false
